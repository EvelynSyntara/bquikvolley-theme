<footer class="site-footer" role="contentinfo" data-section-id="{{ section.id }}">
  <div class="container">
    <div class="footer-grid">
      <!-- Brand Column -->
      <div class="footer-brand" data-reveal>
        <div class="footer-brand__logo">
          {%- if section.settings.logo != blank -%}
            {{ section.settings.logo | image_url: width: 180 | image_tag: loading: 'lazy', alt: shop.name }}
          {%- else -%}
            B<span>QUIK</span>
          {%- endif -%}
        </div>
        <p class="footer-brand__tagline">
          {{ section.settings.tagline | default: "Premium volleyball apparel for players who take the court seriously." }}
        </p>
        <div class="social-links" aria-label="Social media links">
          {%- if section.settings.instagram != blank -%}
            <a href="{{ section.settings.instagram }}" class="social-link" target="_blank" rel="noopener noreferrer" aria-label="Instagram">IG</a>
          {%- endif -%}
          {%- if section.settings.tiktok != blank -%}
            <a href="{{ section.settings.tiktok }}" class="social-link" target="_blank" rel="noopener noreferrer" aria-label="TikTok">TK</a>
          {%- endif -%}
          {%- if section.settings.youtube != blank -%}
            <a href="{{ section.settings.youtube }}" class="social-link" target="_blank" rel="noopener noreferrer" aria-label="YouTube">YT</a>
          {%- endif -%}
          {%- if section.settings.facebook != blank -%}
            <a href="{{ section.settings.facebook }}" class="social-link" target="_blank" rel="noopener noreferrer" aria-label="Facebook">FB</a>
          {%- endif -%}
        </div>
      </div>

      <!-- Link Columns from Blocks -->
      {%- for block in section.blocks -%}
        {%- if block.type == 'link_list' -%}
          <div class="footer-col" data-reveal data-reveal-delay="{{ forloop.index }}" {{ block.shopify_attributes }}>
            <h3 class="footer-col__title">{{ block.settings.heading }}</h3>
            <ul class="footer-col__links">
              {%- for link in linklists[block.settings.menu].links -%}
                <li>
                  <a href="{{ link.url }}" class="footer-col__link">{{ link.title }}</a>
                </li>
              {%- endfor -%}
            </ul>
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>

    <!-- Footer Bottom -->
    <div class="footer-bottom">
      <p class="footer-bottom__copy">
        &copy; {{ 'now' | date: '%Y' }} {{ shop.name }}. All rights reserved.
      </p>
      <nav class="footer-bottom__links" aria-label="Footer legal links">
        {%- for link in linklists['footer'].links -%}
          <a href="{{ link.url }}" class="footer-bottom__link">{{ link.title }}</a>
        {%- endfor -%}
      </nav>
    </div>

    <!-- Payment Icons -->
    <div class="payment-icons" aria-label="Accepted payment methods">
      {%- for type in shop.enabled_payment_types -%}
        <div class="payment-icon">{{ type | payment_type_img_url | img_tag: type }}</div>
      {%- endfor -%}
    </div>
  </div>
</footer>

{% schema %}
{
  "name": "Footer",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "text",
      "id": "tagline",
      "label": "Brand tagline",
      "default": "Premium volleyball apparel for players who take the court seriously."
    },
    {
      "type": "header",
      "content": "Social Links"
    },
    {
      "type": "url",
      "id": "instagram",
      "label": "Instagram URL"
    },
    {
      "type": "url",
      "id": "tiktok",
      "label": "TikTok URL"
    },
    {
      "type": "url",
      "id": "youtube",
      "label": "YouTube URL"
    },
    {
      "type": "url",
      "id": "facebook",
      "label": "Facebook URL"
    }
  ],
  "blocks": [
    {
      "type": "link_list",
      "name": "Link list",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Quick Links"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Menu",
          "default": "footer"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Footer",
      "blocks": [
        { "type": "link_list", "settings": { "heading": "Shop", "menu": "main-menu" } },
        { "type": "link_list", "settings": { "heading": "Help", "menu": "footer" } }
      ]
    }
  ]
}
{% endschema %}
